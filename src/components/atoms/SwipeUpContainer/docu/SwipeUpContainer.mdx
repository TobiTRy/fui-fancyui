import { Meta, Title, Description, Controls } from '@storybook/blocks';

import * as SwipeUpContainerStories from './SwipeUpContainer.stories';

<Meta of={SwipeUpContainerStories} />

<Title />

<Description />

## Setup Instructions

To use the `SwipeUpContainer` in your project, follow these steps:

1. Import the `SpacingContainer` component where you intend to use it:

```jsx
import { SwipeUpContainer } from '@tobitry/fui-fancyui';
```

2. If using TypeScript, ensure the type definitions are imported for prop validation:

```jsx
import { TSwipeUpContainer, TSwipeUpContainerWithHTMLAttrs } from '@tobitry/fui-fancyui';
```

## Example Usage

Here's a simple example of how to use the `SwipeUpContainer` component:

```jsx
<SwipeUpContainer isScalable={true} themeType="secondary" layer={1} style={{ height: '50%' }}>
  <p>Your content goes here</p>
</SwipeUpContainer>
```

This example creates a `SwipeUpContainer` that is scalable, uses the "secondary" theme type, is on layer 1, and has a height of 50%.

## Component Properties

<Controls />

The `TSwipeUpContainer` type defines the props accepted by the `SwipeUpContainer`, ensuring proper types are used for each property to maintain consistency and reliability in usage.
