import { Meta, Canvas, Title, Description } from '@storybook/blocks';
import * as AlignedLabelStory from './AlignedLabel.stories.tsx';
import AlignedWrapper from './AlignedWrapper';
import AlignedLabel from './AlignedLabel';

import { themeStore } from '@/design/theme/themeStore';

<Meta of={AlignedLabelStory} parameters={{ backgrounds: { default: 'light' } }} />

<div style={{
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  padding: '20px',
  background: themeStore.getState().theme.colors.primary[0],
  color: themeStore.getState().theme.colors.secondary[0],

}}>

  <h1>AlignedLabel</h1>
  <AlignedLabel >
    HI
  </AlignedLabel>
  <p>AlignedLabel is a component that aligns its children to the top, bottom, left, right, or center.</p>
</div>

<AlignedWrapper />

<Canvas style={{ background: themeStore.getState().theme.colors.primary[0], width: '100%', height: '100%', display: 'flex', flexDirection: 'column' }}>

<Description />

<Title />

</Canvas>
