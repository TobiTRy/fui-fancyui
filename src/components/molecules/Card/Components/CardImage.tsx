import { FancyImageWrapper, TFancyImageWrapperWithNativeAttrs } from '@/components/atoms/FancyImageWrapper';

export default function CardImage(props: TFancyImageWrapperWithNativeAttrs) {
  return <FancyImageWrapper {...props}></FancyImageWrapper>;
}
