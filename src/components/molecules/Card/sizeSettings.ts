import { TBorderRadiusSizes } from '@/types/TBorderRadiusSizes';
import { TThemeArrayCssValues } from '@/design/designFunctions/arrayToCssValues';

export const sizeSettings = {
  sm: {
    padding: ['xl'] as TThemeArrayCssValues,
    borderRadius: 'sm' as TBorderRadiusSizes,
  },
  md: {
    padding: ['xl'] as TThemeArrayCssValues,
    borderRadius: 'sm' as TBorderRadiusSizes,
  },
  lg: {
    padding: ['xl'] as TThemeArrayCssValues,
    borderRadius: 'xl' as TBorderRadiusSizes,
  },
};
