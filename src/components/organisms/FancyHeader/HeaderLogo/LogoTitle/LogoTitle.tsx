import React from 'react';

import { Typography } from '@/components/atoms/Typography';

export default function LogoTitle(props: React.ComponentProps<typeof Typography>) {
  return <Typography {...props} />;
}
