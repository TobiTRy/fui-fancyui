# clampNumbers

Clamp a numeric value between a minimum and maximum, ensuring it stays within the defined range.

## Introduction

The `clampNumbers` function is a utility for constraining a number. It's helpful for UI elements, calculations, and data normalization.

## Usage

```javascript
import { clampNumbers } from 'fui-fancyui';

const clampedValue = clampNumbers(25, 10, 20); // Returns 20
```

## Props (Arguments)

| Prop Name | Type   | Description                |
| :-------- | :----- | :------------------------- |
| `value`   | number | The value to clamp.        |
| `min`     | number | The minimum allowed value. |
| `max`     | number | The maximum allowed value. |

## Example (Using clamped value in JSX):

```jsx
const clampedWidth = clampNumbers(userInputWidth, 50, 200);

return <div style={{ width: `${clampedWidth}px` }}>...</div>;
```
