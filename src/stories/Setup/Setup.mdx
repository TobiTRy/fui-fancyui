import { Meta } from '@storybook/blocks';

<Meta title="setup/Setup" />

# 🚀 Setup Guide for Fancy UI Library (FUI)

Get ready to elevate your UI game with the `Fancy UI Library (FUI)`! 🌟 Let's dive into the setup process.

## Step 1: Install the Party Guests 📦

Kick things off by inviting `FancyUILibary (FUI)`:

```bash
npm install fui-fancyui
```

`FUI` pairs perfectly with `styled-components`, so let's add them to the mix:

```bash
npm install styled-components
```

`FancyUILibary` is crafted with `StyledComponents` and it relies on `FancyThemeProvider` for its vibrant colors. 🌈

## Step 2: Integrate FancyThemeProvider 🎨

Integrate the `FancyThemeProvider` in your application to manage the theme using the provided `dummyTheme` variable:

```javascript
import { FancyThemeProvider } from '@mrtryy/fancyuilibary';
import { createGlobalStyle } from 'styled-components';

// Define your Global Style
const GlobalStyle = createGlobalStyle`
  body {
    margin: 0;
    padding: 0;
    background: ${({ theme }) => theme.colors.primary[0]};
    color: ${({ theme }) => theme.colors.secondary[0]};
    font-family: DMSans, Helvetica, Sans-Serif;
  }
  // Add additional global styles as needed
`;

const dummyTheme = {
  // Theme configuration goes here
  // ...
};

export default function App({ children }) {
  return (
    <FancyThemeProvider theme={dummyTheme}>
      <GlobalStyle />
      {children}
    </FancyThemeProvider>
  );
}
```

In this setup, the `FancyThemeProvider` is directly using the `dummyTheme` variable as its theme, ensuring a consistent and appealing UI design across your application.

## Step 3: Creating Fancy Applications 🌟

With `FancyThemeProvider` integrated using `dummyTheme`, you're all set to start building your Fancy UI-powered applications! 🎉

## Special Setup for Next.js (v13+)

For Next.js (v13+) users, please refer to the [Next.js Setup Guide for Fancy UI Library](./?path=/docs/stories-setupnext--docs) for detailed instructions.
