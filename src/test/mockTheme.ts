import { breakpoints } from '@/design/theme/brakePoints';
import { uiColors } from '@/design/theme/generateThemeColor/generateThemeColor';
import { borderRadius, typography, spacingPx } from '@/design/theme/designSizes';

export const mockTheme = {
  colors: uiColors,
  spacing: spacingPx,
  borderRadius: borderRadius,
  fontSizes: typography,
  breakpoints: breakpoints,
};
