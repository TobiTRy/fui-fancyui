export type TCssFiltersTypes = {
  brightness?: number;
  contrast?: number;
  grayscale?: number;
  hueRotate?: number; // in degrees
  invert?: number;
  opacity?: number;
  saturate?: number;
  sepia?: number;
  blur?: string; // include units, e.g., '5px'
};
